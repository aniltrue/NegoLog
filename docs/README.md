# Historical HTML documentation

The HTML files in this directory are an older generated snapshot. They omit
current opponent models and API changes, including the abstract estimated
preference migration.

For the current checkout, use the repository [README](../README.md),
[migration notes](../MAINTENANCE.md), and [current documentation sources and
build instructions](../docs-source/README.md). Build the current HTML into a
separate output directory. This notice does not rewrite the historical pages.
